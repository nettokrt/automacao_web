##YAML Template
default: -p html -p hml --publish-quiet


# Reporters
html: --format pretty --format html --out=reports/report.html

# Environments
prd: ENVIRONMENT=prd
hml: ENVIRONMENT=hml

# Browsers
chrome: BROWSER=chrome

# Visual Mode
headless: HEADLESS=true